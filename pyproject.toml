[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mein-projekt"
version = "1.0.0"
description = "Meine PyQt6-Anwendung"
requires-python = ">=3.10"

dependencies = [
    "PyQt6",
    "cryptography",
]