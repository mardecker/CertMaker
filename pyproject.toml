[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "CertMaker"
version = "1.0.0"
description = "Certificate and CSR Creation Tool"
requires-python = ">=3.10"

dependencies = [
    "PyQt6",
    "cryptography",
]